# דוח בקרת נגישות — תעלומה בשכונה (הבהובי רעיונות)
**מודול:** `learning-demo/` · **קבצים:** index.html (817 שורות) / script.js (54,560 תווים) / styles.css (50,220 תווים) · **תאריך:** 2026-08-23
**תקן:** WCAG 2.1 AA + ת"י 5568 · **שיטה:** `scan.py` (סטטי, דטרמיניסטי) + קריאת-קוד שיפוטית מעמיקה (JS logic, CSS rules, DOM structure) על פני **28 המסכים** (`data-screen="1"` עד `"28"`)
**כיסוי:** 28/28 מסכים נסרקו במקור ה-HTML הסטטי (כל התוכן במודול הזה מוזרק לתוך `index.html` מראש, ולא נבנה דינמית ע"י JS מלבד שינויי class/state — כך ש-100% מהמארקאפ קיים בקובץ הסטטי וניתן היה לבדוק כל מסך) · **סיכום במקור (2026-08-23):** 🔴 **9** חוסמים · 🟠 **7** אזהרות · 🔵 **3** ליטוש

> ## ✅ עדכון 2026-08-24 — תוקנו כל הממצאים שלא דורשים שינוי-נראות
> לפי בקשת המפתח/ת, בוצעו תיקוני קוד (HTML/CSS/JS) לכל ממצא שניתן לתקן **מבלי לשנות את המראה החזותי
> הקיים** של הלומדה (ראה שורה "✅ תוקן ..." בכל ממצא רלוונטי למטה). **סיכום לאחר תיקון: 🔴 5 חוסמים ·
> 🟠 4 אזהרות · 🔵 1 ליטוש** — הנותרים מפורטים למטה, וכולם דורשים החלטת-עיצוב חזותית (צבע/אייקון/רכיב
> נראה חדש) או החלטת-תוכן/ארכיטקטורה, ולכן לא נגעתי בהם. `scan.py` הורץ מחדש לאחר התיקונים ומאשר: #11
> עבר מ"46 מתוך 56 בלי tabindex" ל-**"0 מתוך 56 בלי tabindex"**, ו-#3/#4/#30 לא מופיעים יותר בפלט הסורק.
>
> **נותר לתקן (דורש שינוי-נראות, לא בוצע):**
> - #23 ניגודיות (2 ממצאים ודאיים) — דורש שינוי-צבע.
> - #25 (א)+(ב) צבע-כסמן-בלעדי — דורש הוספת אייקון ✓/✗ נראה.
> - #8/#10 וידאו-רקע ללא עצירה (מסך 8) — כפתור-עצירה נגיש חייב להיות נראה כשמתמקדים בו.
> - #9 כתוביות ל-`<track>` native, #26 ארכיטקטורת-קנבס/reflow, #32 הצהרת-נגישות — דורשים החלטת
>   תוכן/ארכיטקטורה, לא רק markup.

> ## ⚠️ מגבלת בדיקה קריטית — נא לקרוא לפני השימוש בדוח
> **סביבת ההרצה של דוח זה לא כללה כלי דפדפן/preview חיים (`preview_*`) ולא הצלחתי להזריק axe-core, לביים
> Tab-walk אמיתי, או למדוד ניגודיות מחושבת (`getComputedStyle`) על ה-DOM החי.** לכן, בשונה מהמתודולוגיה
> המלאה של סקיל זה (שדורשת גם ביקורת דינמית ב-preview), **דוח זה מבוסס על:**
> 1. סריקה דטרמיניסטית (`scan.py`) על index.html/styles.css/script.js.
> 2. קריאה שיפוטית מעמיקה של כל 28 המסכים ב-HTML, וקריאת מלוא הלוגיקה הרלוונטית ב-script.js (ניווט,
>    מאזיני-מקלדת, ניהול-מצב ARIA, ניהול-פוקוס, מודלים, גרירה, וידאו/כתוביות) כדי להסיק מה *בפועל* יקרה
>    בדפדפן חי, ואימות הדדי בין ה-HTML למאזיני ה-JS (למשל: איתור כל מאזיני ה-`keydown` הקיימים בקובץ
>    ואימות שהם *לא* מכסים רכיבי `role="button"` מסוימים).
> 3. **בדיקות שדורשות DOM חי ולא ניתן היה לבצע בפועל** (מסומנות "❓ לא נבדק דינמית" בטבלת-הכיסוי):
>    - ניגודיות מחושבת בפועל (רקעי-gradient/תמונה עשויים לשנות את התוצאה בפועל).
>    - סדר-Tab אמיתי וחיווי-פוקוס נראה בפועל על כל רכיב.
>    - reflow בפועל ב-320px / זום 200% (ההערכה כאן היא ניתוח-ארכיטקטורה מהקוד, לא מדידה).
>    - axe-core (מכסה ~30–40% מהבעיות המכניות שהניתוח הידני-שיפוטי לא תמיד תופס).
>
> **המשמעות: יש להריץ סבב-אימות דינמי בדפדפן (axe-core + Tab-walk + ניגודיות מחושבת + reflow) לפני
> אישור-נגישות סופי.** כל הממצאים להלן מבוססי-קוד ומצוטטים במדויק, אך רשימת ה-❓ בטבלת-הכיסוי בתחתית
> אינה "תקין" — היא "לא נבדק". **אישור-נגישות סופי דורש גם בדיקה עם קורא-מסך אנושי (NVDA/VoiceOver).**

---

## ממצאים לפי קטגוריה

### א. מבנה ושפת המסמך

| סוג הבעיה | מיקום (file:line) | ציטוט / מה נמצא | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| ✅ תוקן — #3 היעדר כותרות סמנטיות | כל 28 המסכים ב-`index.html` (לדוגמה `.q-title` ב-index.html:118,195,261,298,335,372,446,484,576,606,658,745; `.intro-title-line1/2` ב-index.html:16-17) | ~~אין תג `<h1>`–`<h6>` יחיד בכל הקובץ~~ — **תוקן:** `.intro-title-line2` הוא כעת `<h1>` (כותרת המודול, מסך 1), וכל 13 מופעי `.q-title` הם כעת `<h2>` — ה-CSS הקיים לכל class כבר קבע `font-size`/`font-weight`/`margin` מפורשים, כך שהמראה החזותי לא השתנה | — | ניווט-לפי-כותרות בקורא-מסך עובד ל-14 כותרות (WCAG 1.3.1 A) |
| ✅ תוקן — #4 אין landmark ראשי | `index.html:10` (`<div id="app">`) | ~~אין `<main>`/`role` תואם~~ — **תוקן:** נוסף `role="main"` ל-`#app` (attribute בלבד, ללא השפעה על רינדור) | — | קורא-מסך יכול לדלג ישירות לתוכן העיקרי (WCAG 1.3.1 A) |
| ✅ #1 שפת מסמך | `index.html:2` | `<html lang="he" dir="rtl">` — תקין | — | — |
| ✅ #2 כותרת דף | `index.html:6` | `<title>תעלומה בשכונה — הבהובי רעיונות</title>` — מתארת וייחודית | — | — |
| ✅ #5 מזהים כפולים | כל הקובץ | לא נמצאו `id` כפולים (למרות ~11 מסכי-שאלה החולקים מבנה זהה, ה-`id` שלהם ייחודי לכל מסך: `q-check-9`, `q-check-12` וכו') | — | — |

### ב. תמונות ומדיה

| סוג הבעיה | מיקום (file:line) | ציטוט / מה נמצא | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| 🔴 #8 תוכן-נע רץ ללא הגבלת-זמן/עצירה | `index.html:169` — `<video class="screen-bg-video" src="assets/videos/video_page_8.mp4" autoplay muted loop playsinline></video>`, ומופעל-מחדש תכנותית בכל כניסה למסך ב-`script.js:336-345` (`resetScreen8`: `video.currentTime=0; video.play()`) | וידאו-רקע דקורטיבי במסך 8 מתנגן אוטומטית, ב-**לופ אינסופי**, ואין שום כפתור/פקד לעצירה או השהיה שלו בכל המודול | **הוסיפו** כפתור "השהה רקע" נגיש (`aria-label`), או **הגבילו** את הלופ למספר חזרות סופי שנפסק אוטומטית, או **הסירו** `loop` והחליפו בפריים אחרון קבוע | תוכן נע לא ירדוף משתמשים רגישי-תנועה/ריכוז לאורך כל זמן השהייה במסך (WCAG 2.2.2 A — חובה גם בת"י 5568) |
| 🟠 #10 autoplay של מדיה | `index.html:169` | ה-`autoplay` על הווידאו הנ"ל (מוצמד ל-`muted` — כך שלא מפר את 1.4.2 שעניינה אודיו, אך מחריף את #8 לעיל) | **ראו המלצת #8** — הבעיה המעשית כאן היא העדר-עצירה, לא האודיו (שמושתק) | — (ראו #8) |
| 🔵 #9 כתוביות-וידאו — לא ב-`<track>` תקני | `index.html:33,101,159,223,410,640` (שישה תגי `<video>`) + `script.js:172-231` (`VIDEO_CAPTIONS`/`setupVideoCaptions`) | **בניגוד לדגל האוטומטי של הסורק** — נמצא בפועל מנגנון-כתוביות מותאם: `VIDEO_CAPTIONS` מכיל cues מתוזמנים (start/end/טקסט) לכל 6 מסכי-הווידאו (2,5,7,10,16,24), ומוזרקים ל-`.video-caption p` בזמן-אמת לפי `timeupdate`. זו **חלופה פונקציונלית סבירה** לכתוביות, אך: (1) לא ניתנת-לכיבוי/הפעלה ע"י המשתמש (אין כפתור CC), (2) לא נחשפת ל-API-נגישות של הדפדפן כ-`<track>` אמיתי | **שקלו** להמיר ל-`<track kind="captions" srclang="he">` אמיתי (מאפשר כיבוי ע"י המשתמש ותאימות-נגישות מובנית), או להוסיף כפתור-הצג/הסתר-כתוביות למנגנון הקיים | שוויון-בקרה על הכתוביות ותאימות טובה יותר לכלי-נגישות (WCAG 1.2.2 A) |
| ✅ #6 טקסט חלופי (alt) | כל 28 המסכים | נבדקו כל תגי ה-`<img>` בקובץ: תמונות דקורטיביות (`Background*.jpg/.jpeg`, `textbox1*.png`, אייקוני `sim-reopen-btn`) מקבלות `alt=""` נכון; תמונות תוכניות (`q3_picture.png` ב-index.html:273-274,310-311,347-348,384-385, `q4_picture.png` ב-index.html:459-460,496-497) מקבלות alt מתאר-נתונים מלא (למשל "גרף קווי המציג... עולה עד ל-120 מעלות... יורדת בהדרגה עד ל-25...") הכולל את הערכים המספריים הנדרשים לפתרון השאלה — לא רק תיאור טופוגרפי | — | — |
| ➖ #7 טקסט מהותי בתוך תמונה — לא ניתן לאימות מלא | תמונות תוכן (`q3_picture.png`, `q4_picture.png`, `q2_picture.png`, `q7_picture.png`) | לא בוצעה בדיקה ויזואלית של תוכן הפיקסלים בפועל (מחוץ לתחום ניתוח-קוד); אולם alt-הטקסט הקיים (#6) כבר משכפל את הערכים המספריים המהותיים בטקסט חי, מה שממתן את הסיכון | **מומלץ**: אימות ויזואלי חד-פעמי שאין בתמונות טקסט/נתון נוסף שלא מופיע ב-alt | — |
| ➖ #8 (GIF מונפש) — לא רלוונטי | — | לא נמצא אף קובץ `.gif` בתיקיית `assets/` או הפניה ל-`.gif` בקוד | — | — |

### ג. מקלדת ופוקוס — הליבה הקריטית ללומדה

| סוג הבעיה | מיקום (file:line) | ציטוט / מה נמצא | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| ✅ תוקן — #11 (א) `.q-opt` היה בלתי-נגיש למקלדת כליל | `index.html` — **46 מופעים** ב-11 מסכי-שאלה (9,12,13,14,15,18,19,21,22,25,27) | ~~`<div class="q-opt">` בלי `tabindex`/`role`, בלי מאזין-מקלדת~~ — **תוקן:** כל 46 האופציות קיבלו `role="radio"`/`"checkbox"` (לפי single/multi), `tabindex="0"`, `aria-checked` מסונכרן חי (ב-`qSelect`/`qMarkOptions`/`qEnter`), ועטיפת `.q-answers` קיבלה `role="radiogroup"`/`"group"`. מאזין `keydown` משותף חדש מפעיל Enter/Space כ-click. `scan.py` מאשר "0 מתוך 56 בלי tabindex" | — | 11 מתוך 27 מסכי-תוכן הפכו לפתירים במקלדת/קורא-מסך (WCAG 2.1.1 A + 4.1.2 A) |
| ✅ תוקן — #11 (ב) `.rtg-tile-round`/`.rtg-flipcard` — ARIA תקין אך לא הופעל במקלדת | `index.html` — 10 מופעים (מסכים 4, 20) | ~~`role="button"` על `<div>` לא מקבל Enter/Space אוטומטית, ואין מאזין ב-JS שמפעיל `rtgClick`~~ — **תוקן:** מאזין `keydown` גלובלי חדש (`script.js`) מפעיל `.click()` על כל `role="button"/"radio"/"checkbox"` ב-Enter/Space | — | 10 כרטיסים במסכי-מפתח (4, 20) ניתנים-להפעלה במקלדת (WCAG 2.1.1 A) |
| ✅ תוקן — #33 גרירה ללא חלופת-מקלדת (הייתה הבדיקה הקריטית ביותר) | `index.html` (6 כרטיסי-מקור, 6 אזורי-שחרור, מסך 26) + `script.js` (`ddqKeyPick`/`ddqKeyDrop`) | ~~HTML5 drag-native בלעדי, ללא `tabindex`/`role`/`aria-label`, ללא מסלול חלופי~~ — **תוקן:** כל כרטיס קיבל `role="button"` + `tabindex="0"` + `aria-label` דינמי; זרימת מקלדת מלאה — Enter על כרטיס = "הרמה" (עם `.selected` — סטייל שהיה כבר קיים ב-CSS ולא בשימוש), Enter על תא ריק = "הצבה", עם הכרזות ב-`aria-live` דרך אזור-סטטוס חדש ומוסתר-ויזואלית (`#a11y-status`, `.sr-only`) | — | שאלה 8 פתירה כעת במקלדת/קורא-מסך (WCAG 2.1.1 A + 4.1.2 A) |
| ✅ תוקן — #12 מודלים נפתחו "שקטים" — בלי ניתוב-פוקוס/לכידה | `index.html` (`#feedback-popup`, `#img-zoom-modal`, `#sim-reopen-popup`) + `script.js` | ~~אין `.focus()` בכל הקובץ; אין לכידת-Tab~~ — **תוקן:** נוספו `role="alertdialog"/"dialog"` + `aria-modal="true"` + `tabindex="-1"`; פונקציות `openOverlayFocus`/`returnOverlayFocus` מעבירות פוקוס לתוך המודל בפתיחה ומחזירות לכפתור-הפותח בסגירה; מאזין ה-`keydown` הגלובלי לוכד Tab בתוך המודל הפתוח | — | קורא-מסך ומשתמש-מקלדת יודעים שמודל נפתח ולא "נעלמים" ממנו (WCAG 2.1.2 A + 2.4.3 A) |
| ✅ תוקן — #16 אין ניתוב-פוקוס במעבר מסך | `script.js` (`resetScreenState`) | ~~אין `.focus()` במעבר בין מסכים~~ — **תוקן:** כל 28 ה-`<section class="screen">` קיבלו `tabindex="-1"`, ונוספה `focusScreen(n)` שרצה בסוף `resetScreenState` (מכוסה גם עבור ניווט חוץ-`goTo`) | — | משתמש-מקלדת/SR מקבל אוריינטציה מיידית במסך החדש (WCAG 2.4.3 A) |
| ✅ #13 חיווי פוקוס נראה (הערכת-קוד) | כל `styles.css` | לא נמצא שום `outline:none`/`outline:0` גלובלי או על רכיבים אינטראקטיביים — ה-outline הדפדפני המובנה נשאר פעיל על כפתורים; ל-`.rtg-tile-round` יש גם עיצוב `:focus-visible` מפורש (`styles.css:530-531`) | — | לא אומת חזותית בדפדפן חי — ראו אזהרת-הבדיקה |
| ✅ #15 tabindex חיובי | כל הקובץ | לא נמצא שום `tabindex` בערך גדול מ-0 | — | — |
| ❓ #14 סדר-טאב הגיוני | כל המסכים | דורש Tab-walk בדפדפן חי; לא בוצע בסביבה זו | ראו אזהרת-הבדיקה למעלה | — |

### ד. ARIA ומצבים

| סוג הבעיה | מיקום (file:line) | ציטוט / מה נמצא | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| ✅ תוקן — #19 (א) מצב-בחירה/נכון-שגוי לא השתקף ב-ARIA — `.q-opt` | `script.js` (`qSelect`, `qMarkOptions`, `qEnter`) | ~~רק `classList` מתעדכן, אין `aria-checked`~~ — **תוקן:** `aria-checked` מסונכרן בכל שינוי-class מקביל (בחירה, איפוס-כניסה-למסך, וגילוי-סופי) | — | קורא-מסך יודע איזו תשובה נבחרה (WCAG 4.1.2 A) |
| ✅ תוקן — #19 (ב) מצב-בחירה לא השתקף ב-ARIA — `.q1-toggle` | `index.html` (14 כפתורים, מסך 6) + `script.js` (`q1Toggle`, `q1Enter`) | ~~מצב `selected` מסונכרן רק ל-`classList`, לעולם לא ל-ARIA~~ — **תוקן:** נוסף `aria-pressed="false"` ראשוני ל-14 הכפתורים ב-HTML, וסונכרן חי ב-`q1Toggle`/`q1Enter` | — | קורא-מסך מכריז אם סומן "מדעי" עבור שורה נתונה (WCAG 4.1.2 A) |
| ✅ תוקן — #22 הודעות-משוב דינמיות היו בלי live-region | `index.html` (`#feedback-popup`) + `script.js` (`showFeedback`/`hideFeedback`) | ~~אין `aria-live`/`role="dialog"` על `#feedback-popup`~~ — **תוקן:** נוסף `role="alertdialog" aria-modal="true" aria-labelledby="fb-title" tabindex="-1"`, ופוקוס עובר לפופאפ בפתיחה (ראה #12) | — | קורא-מסך מכריז "נכון!"/"לא נכון, נסו שוב" בכל אחת מ-11 השאלות (WCAG 4.1.3 AA) |
| ✅ #17 שם נגיש לרכיבים פעילים אחרים | כל 28 המסכים | נבדקו: כפתורי-ניווט (`aria-label="חזרה"/"המשך"`), כפתורי-וידאו (`aria-label="הפעל סרטון"`), כפתורי-מיון/בדיקה (טקסט גלוי: "האם צדקתי?", "מדעי", "לא מדעי"), כפתור-הגדלה (`aria-label="הגדלת התמונה"`), כפתורי-סגירה (`aria-label="סגירה"`), `sim-reopen-btn` (שם מ-`<span class="sim-reopen-btn-label">סימולציה</span>` הפנימי) — כולם בעלי שם-נגיש תקין | — | — |
| ✅ #18 role תקין ותואם | `index.html:65-92,516,530,544` | ערכי ה-`role="button"` על `.rtg-tile-round`/`.rtg-flipcard` תקינים לשונית (אף אם לא-מופעלים במקלדת — ראו #11(ב)); לא נמצא `role` מיותר על אלמנט native | — | — |
| ✅ #21 aria-hidden לא על רכיב פעיל | `index.html:59,116,655,774` (`.rtg-teacher-char`, `.q1-picture`, `.q7-picture`, `#q-stepper`) | כל שימושי `aria-hidden="true"` הם על `<div>`/`<img>` דקורטיביים או עוטפים לא-אינטראקטיביים — לא על רכיב ניתן-למיקוד | — | — |
| ➖ #20 aria-* מצביע ל-id | כל הקובץ | לא נמצא שום שימוש ב-`aria-labelledby`/`aria-describedby`/`aria-controls` בקובץ — לא רלוונטי | — | — |

### ה. צבע וניגודיות

| סוג הבעיה | מיקום (file:line) | ציטוט / מה נמצא | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| 🔴 #23 ניגודיות טקסט — תג "נצפה" (✓) | `styles.css:553-566` (`.rtg-tile-round .rtg-check`) | `color:#fff` על `background:#1FC123`, `font-size:16px` (משקל רגיל) → יחס-ניגודיות **2.41:1** מחושב, נדרש **4.5:1** לטקסט בגודל זה | **הכהו** את הרקע (למשל `#1B8A1D`, ≈4.6:1 מול לבן) או הגדילו/העיבו את הפונט לחריגה לסף הגדול (3:1) | סימן ה-✓ המציין "כרטיס נצפה" קריא ללקויי-ראייה (WCAG 1.4.3 AA) |
| 🔴 #23 ניגודיות טקסט — תיבת-הגדלה ריקה | `styles.css:1220` (`#img-zoom-panel-body`, מוזרק ע"י `script.js:141` כ-fallback כשלתמונה אין `data-zoom-src`) | `color:#888` על רקע `#fff` (`.img-zoom-panel` — `styles.css:1211`), `font-size:18px` רגיל → **3.54:1**, נדרש **4.5:1** | **הכהו** ל-`#595959` (≈7:1) או `#767676` (≈4.5:1 גבולי) | טקסט-הגיבוי בחלון ההגדלה קריא (WCAG 1.4.3 AA) |
| 🔴 #25 (א) צבע כסמן בלעדי — `.q1-toggle` | `styles.css:1456-1459` (`.q1-toggle.correct`/`.wrong`) | סימון תשובה נכונה/שגויה בטבלת-המיון (מסך 6, 14 כפתורים) הוא **בלעדית** צבע: מסגרת+מילוי ירוק (`var(--green-80)`) לעומת אדום (`var(--red-100)`) — **בדיוק זוג-הצבעים הבעייתי ביותר לעיוורי-צבעים אדום-ירוק** (הצורה, גודל וה-`aria-label` הסטטי זהים בשני המצבים) | **הוסיפו** אייקון (✓/✗) בתוך העיגול בנוסף לצבע, בעזרת `content` ב-`::after` או SVG מוזרק | משתמשים עם עיוורון-צבעים (הנפוץ ביותר) מבחינים בין "נכון" ל"שגוי" (WCAG 1.4.1 A — חובה גם בת"י 5568) |
| 🔴 #25 (ב) צבע כסמן בלעדי — `.q-opt` | `styles.css:891-894` (`.q-opt.correct`/`.wrong`) | בכל 11 מסכי-השאלה: תשובה שנבחרה-ונכונה מקבלת רקע/מסגרת ירוקים, שנבחרה-ושגויה — אדומים; אין הבדל טקסטואלי/אייקוני נוסף בין המצבים | **הוסיפו** אייקון ✓/✗ נראה לצד הטקסט בכל אופציה שסומנה, בנוסף לצבע-הרקע | אותה הנמקה כ-(א) עבור 11 מסכי-השאלה (WCAG 1.4.1 A) |
| 🟠 #26 ארכיטקטורת קנבס קבוע — סיכון-reflow מובנה | `styles.css:32-54` (`html,body{overflow:hidden}`, `#app{width:1920px;height:1080px}`) + `script.js:18-27` (`scaleApp`) | הקנבס שלם מוקבע ל-1920×1080px ומוצג בקנה-מידה יחיד (`transform:scale(min(w/1920,h/1080))`) ביחס לחלון; `overflow:hidden` על `html,body` חוסם כל גלילה. בפועל: **אין reflow אמיתי** — בכל רוחב (כולל 320px) התוכן רק מוקטן פרופורציונלית ("shrink-to-fit"), לרבות הטקסט, שלא ניתן להגדיל עצמאית ללא הגדלת כל הקנבס | **תעדו/בדקו בפועל**: בזום 200% ו-320px, ודאו שהטקסט עדיין קריא בגודל-מסך אמיתי (לא רק "לא נחתך") ושלא נוצרת פסי-ריק (letterboxing) שמסתירים תוכן קריטי; שקלו breakpoint למובייל שמפעיל פריסה זורמת נפרדת | תוכן קריא ונגיש גם במכשירים צרים/בהגדלה (WCAG 1.4.10 AA — חובה גם בת"י 5568). **לא אומת חזותית בדפדפן חי — ראו אזהרת-הבדיקה** |
| ✅ #27 חסימת-זום ב-viewport | `index.html:5` | `<meta name="viewport" content="width=device-width, initial-scale=1.0">` — אין `user-scalable=no`/`maximum-scale` | — | — |
| ✅ #23 (תיקון-חיוב לדגל שגוי של הסורק) — `.q-step` | `styles.css:682-697` | הסורק הסטטי דגל `#299FD4`/`#fff` (יחס 3.00:1) כ-🟠 לפי סף 4.5:1 של טקסט-רגיל — **אך** הטקסט (מספרי-שלבים 1-9) הוא `font-size:34px; font-weight:700` (index.html:775-783 + styles.css:690-692), כלומר "טקסט גדול" שסובל בשל **3:1** בלבד. 3.00:1 עומד **בדיוק** בסף (עובר, גבולי) | **מומלץ (לא חוסם)**: הוסיפו שוליים קטנים (למשל `#1E8AB8`, ≈3.3:1) כרווח-בטיחות מפני שגיאת-עיגול | — |
| ❓ #24 ניגודיות רכיבים גרפיים | גבולות/אייקוני-בחירה בכל המסכים | דורש בדיקה חזותית/`getComputedStyle` על DOM חי; לא בוצע | ראו אזהרת-הבדיקה | — |

### ו. טפסים ואינטראקציה

| סוג הבעיה | מיקום (file:line) | ציטוט / מה נמצא | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| ✅ תוקן — #30 היה אין `prefers-reduced-motion` | `styles.css` (סוף הקובץ) | ~~אין בלוק `@media (prefers-reduced-motion: reduce)`~~ — **תוקן:** נוסף בלוק שמכבה את `animation`/`transition` של `.nav-btn.blink`/`.rtg-flipcard-face` למשתמשים שהפעילו את ההעדפה ברמת מערכת-ההפעלה בלבד — אינו משפיע על אף משתמש אחר | — | נוחות למשתמשים רגישי-תנועה (WCAG 2.3.3 AAA) |
| ✅ #31 היבהוב מהיר | `styles.css:212` | קצב ה-blink הוא 1.1 שניות למחזור מלא (≈0.9Hz) — הרבה מתחת לסף-הסיכון של 3Hz | — | — |
| ➖ #28 תווית לשדה-קלט | — | לא נמצא שום `<input>`/`<textarea>`/`<select>` בכל המודול — לא רלוונטי | — | — |
| ➖ #29 הודעת שגיאה נגישה | — | אין טפסים/שדות-קלט הדורשים ולידציה; משוב-תשובה מטופל תחת #22 | — | — |

### ז. ת"י 5568 — תוספות ישראליות

| סוג הבעיה | מיקום (file:line) | ציטוט / מה נמצא | המלצת תיקון | מה הפתרון פותר |
|---|---|---|---|---|
| 🟠 #32 הצהרת נגישות ודרך-פנייה | כל המודול | לא נמצא קישור/טקסט להצהרת-נגישות או לפרטי רכז/ת-נגישות בתוך `index.html` | **אמתו מול פלטפורמת-האם** (מוטמע ב-iframe/SCORM) שההצהרה קיימת בשם אתר-האם; אם המודול עומד-בפני-עצמו — הוסיפו קישור | עמידה בדרישת-הפנייה של תקנות שוויון-זכויות/ת"י 5568 |
| 🟠 RTL/עברית — הבחנה כללית | כל הקובץ | `lang="he" dir="rtl"` תקין גלובלית (#1); לא בוצעה בדיקה ממוקדת של bidi בתוכן מעורב-כיוון (מספרים בתוך משפטים עבריים, כגון "120 מעלות צלזיוס" ב-alt-טקסטים) מול קורא-מסך בפועל | **מומלץ** בדיקת-NVDA ממוקדת לקטעי alt המכילים מספרים בתוך טקסט עברי | קריאה תקינה של ערכים מספריים בהקשר עברי |

---

## סיכום מנחה למפתח/ת — 3 התיקונים בעלי ההשפעה הגבוהה ביותר
1. **#11(א) + #19(א) + #22 (מסכי-שאלה):** הפיכת `.q-opt` לרכיבים אמיתיים עם role/state/keyboard — זה לבדו פותר כשל שחוסם **11 מתוך 27 מסכי-תוכן** ממשתמשי-מקלדת/קורא-מסך.
2. **#33 (מסך 26):** חלופת-מקלדת לתרגיל-הגרירה — בלעדיה, מסך שלם בלתי-פתיר במקלדת.
3. **#12 + #22 (מודלים/משוב):** ניתוב-פוקוס + `aria-live`/`role="alertdialog"` לחלון-המשוב — משפיע על **כל** 11 מסכי-השאלה.

## טבלת כיסוי — כל 33 הבדיקות

| # | בדיקה | קריטריון | סטטוס | מיקומים |
|:--:|---|---|---|---|
| 1 | שפת מסמך | 3.1.1 | ✅ | index.html:2 |
| 2 | כותרת דף | 2.4.2 | ✅ | index.html:6 |
| 3 | סדר כותרות | 1.3.1 | ✅ תוקן | h1×1, h2×13 |
| 4 | landmarks | 1.3.1 | ✅ תוקן | index.html:10 (`role="main"`) |
| 5 | מזהים כפולים | 4.1.1/1.3.1 | ✅ | — |
| 6 | טקסט חלופי (alt) | 1.1.1 | ✅ | — |
| 7 | טקסט בתוך תמונה | 1.4.5 | ➖ לא-אומת-חזותית | q3/q4_picture.png |
| 8 | תוכן נע / GIF | 2.2.2 | 🔴 ממצא (וידאו-רקע, לא GIF) | index.html:169 |
| 9 | כתוביות וידאו | 1.2.2 | 🔵 ממצא (חלופה קיימת, לא native) | index.html:33,101,159,223,410,640 |
| 10 | autoplay מדיה | 1.4.2 | 🟠 ממצא (מושתק) | index.html:169 |
| 11 | הפעלה במקלדת | 2.1.1 | ✅ תוקן — 56/56 מופעים | ראה טבלה ג' |
| 12 | מלכודת/ניתוב-מודל | 2.1.2/2.4.3 | ✅ תוקן | index.html (feedback/zoom/sim popups) |
| 13 | חיווי פוקוס נראה | 2.4.7 | ✅ (הערכת-קוד) | — |
| 14 | סדר טאב הגיוני | 2.4.3 | ❓ לא נבדק דינמית | — |
| 15 | tabindex חיובי | 2.4.3 | ✅ | — |
| 16 | פוקוס על תוכן מוזרק | 2.4.3 | ✅ תוקן | script.js (`focusScreen`) |
| 17 | שם נגיש | 4.1.2 | ✅ | — |
| 18 | role תקין | 4.1.2 | ✅ | — |
| 19 | מצב משתקף ב-ARIA | 4.1.2 | ✅ תוקן | script.js (`qSelect`/`qMarkOptions`/`q1Toggle`) |
| 20 | aria-* → id קיים | 1.3.1 | ➖ לא-רלוונטי (לא בשימוש) | — |
| 21 | aria-hidden לא-על-פעיל | 4.1.2 | ✅ | — |
| 22 | live regions | 4.1.3 | ✅ תוקן | index.html (`#feedback-popup`) |
| 23 | ניגודיות טקסט | 1.4.3 | 🔴 ממצא (2 ודאיים) + ✅ (2 דגלים כוזבים מתוקנים) | styles.css:553-566,1220 |
| 24 | ניגודיות רכיבים גרפיים | 1.4.11 | ❓ לא נבדק דינמית | — |
| 25 | צבע כסמן בלעדי | 1.4.1 | 🔴 ממצא | styles.css:891-894,1456-1459 |
| 26 | reflow/zoom | 1.4.4/1.4.10 | 🟠 ממצא (סיכון-ארכיטקטורה) | styles.css:32-54 |
| 27 | חסימת זום viewport | 1.4.4 | ✅ | index.html:5 |
| 28 | תווית לשדה קלט | 1.3.1/3.3.2 | ➖ לא-רלוונטי (אין טפסים) | — |
| 29 | הודעת שגיאה נגישה | 3.3.1 | ➖ לא-רלוונטי (אין טפסים) | — |
| 30 | reduced-motion | 2.3.3 | ✅ תוקן | styles.css (סוף הקובץ) |
| 31 | היבהוב | 2.3.1 | ✅ | styles.css:212 |
| 32 | הצהרת נגישות | ת"י 5568 | 🟠 ממצא | — |
| 33 | גרירה ללא חלופת-מקלדת | 2.1.1/4.1.2 | ✅ תוקן | index.html + script.js (`ddqKeyPick`/`ddqKeyDrop`) |
